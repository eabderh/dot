#!/bin/bash

# preserve paths with spaces
args=( "$@" )

# filter all files for only text files, change multiline to single line
files="$(textfiles "${args[@]}" | tr '\n' ' ')"

if [[ "$files" == "" ]]; then
	exit 0
fi

exec $EDITOR $files

